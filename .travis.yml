language: c

branches:
  only:
    - master

notifications:
  email: false

script:
  - echo "HELLO" > index.html

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  keep_history: true
  on:
    branch: gh-pages